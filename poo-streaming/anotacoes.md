git config --global user.email "miquelaoo@gmail.com"

git init

git add .

git status

git commit -m

$ git reset --soft HEAD~1 --> volta um commit

git reset --> volta o git add

path de arquivo: cd ~/..

Regra de nomenclatura: dominio invertido. (novo pacote --> dominio invertido)
ex: br.com.streaming.model (dentro de src)